// import { expect } from 'chai'; // eslint-disable-line import/no-extraneous-dependencies
// import Library from '../../../src';
// import Recorder from '../../../scripts/recorder';

// const rec = new Recorder('licensing');

// const baseurl = process.env.IMAGERIGHT_BASEURL || 'https://localhost:8093';
// const username = process.env.IMAGERIGHT_USERNAME || '<username>';
// const password = process.env.IMAGERIGHT_PASSWORD || '<password>';

// let lib;

describe('ImageRight API - Licensing', () => {
  // before(() => {
  //   rec.before();
  //   lib = new Library(baseurl).connect(username, password);
  // });

  // after((done) => {
  //   rec.after(done);
  // });

  describe('the method getConcurrentLoginData', () => {
    it('should return login info');
  });

  describe('the method getMessages', () => {
    it('should return messages');
  });

  describe('the method login', () => {
    it('should return login info');
  });

  describe('the method logout', () => {
    it('should logout the session');
  });

  describe('the method stillAlive', () => {
    it('should return the alive info');
  });

  describe('the method validateLicense', () => {
    it('should return validate a license');
  });
});
