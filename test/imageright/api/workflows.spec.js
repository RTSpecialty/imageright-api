// import { expect } from 'chai'; // eslint-disable-line import/no-extraneous-dependencies
// import Library from '../../../src';
// import Recorder from '../../../scripts/recorder';

// const rec = new Recorder('images');

// const baseurl = process.env.IMAGERIGHT_BASEURL || 'https://localhost:8093';
// const username = process.env.IMAGERIGHT_USERNAME || '<username>';
// const password = process.env.IMAGERIGHT_PASSWORD || '<password>';

// let lib;

describe('ImageRight API - Images', () => {
  // before(() => {
  //   rec.before();
  //   lib = new Library(baseurl).connect(username, password);
  // });

  // after((done) => {
  //   rec.after(done);
  // });

  describe('the method getDefaultStepLink', () => {
    it('should return a...');
  });

  describe('the method getPriorityList', () => {
    it('should return a...');
  });

  describe('the method getSplitLinkParameters', () => {
    it('should return a...');
  });

  describe('the method getStepAttributeById', () => {
    it('should return a...');
  });

  describe('the method getStepAttributeByName', () => {
    it('should return a...');
  });

  describe('the method getStepAttributes', () => {
    it('should return a...');
  });

  describe('the method getStepLinks', () => {
    it('should return a...');
  });

  describe('the method getSteps', () => {
    it('should return a...');
  });

  describe('the method getUsersToAssign', () => {
    it('should return a...');
  });

  describe('the method getWorkflows', () => {
    it('should return a...');
  });
});
