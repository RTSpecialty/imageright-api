// import { expect } from 'chai'; // eslint-disable-line import/no-extraneous-dependencies
// import Library from '../../../src';
// import Recorder from '../../../scripts/recorder';

// const rec = new Recorder('users');

// const baseurl = process.env.IMAGERIGHT_BASEURL || 'https://localhost:8093';
// const username = process.env.IMAGERIGHT_USERNAME || '<username>';
// const password = process.env.IMAGERIGHT_PASSWORD || '<password>';

// let lib;

describe('ImageRight API - Users', () => {
  // before(() => {
  //   rec.before();
  //   lib = new Library(baseurl).connect(username, password);
  // });

  // after((done) => {
  //   rec.after(done);
  // });

  describe('the method changeUserPassword', () => {
    it('should return a... ');
  });

  describe('the method getCurrentUser', () => {
    it('should return a... ');
  });

  describe('the method getCurrentUserData', () => {
    it('should return a... ');
  });

  describe('the method getUserData', () => {
    it('should return a... ');
  });
});
