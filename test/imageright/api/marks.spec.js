// import { expect } from 'chai'; // eslint-disable-line import/no-extraneous-dependencies
// import Library from '../../../src';
// import Recorder from '../../../scripts/recorder';

// const rec = new Recorder('marks');

// const baseurl = process.env.IMAGERIGHT_BASEURL || 'https://localhost:8093';
// const username = process.env.IMAGERIGHT_USERNAME || '<username>';
// const password = process.env.IMAGERIGHT_PASSWORD || '<password>';

// let lib;

describe('ImageRight API - Marks', () => {
  // before(() => {
  //   rec.before();
  //   lib = new Library(baseurl).connect(username, password);
  // });

  // after((done) => {
  //   rec.after(done);
  // });

  describe('the method getFileMarkDefinitions', () => {
    it('should return an ');
  });

  describe('the method getFileMarkDefinitionsByFileType', () => {
    it('should return an ');
  });

  describe('the method getFileMarkDefinitionsByFileTypeV2', () => {
    it('should return an ');
  });

  describe('the method getFileMarkDefinitionsV2', () => {
    it('should return an ');
  });

  describe('the method getImageDataForFileMark', () => {
    it('should return an ');
  });

  describe('the method getImageDataForPageMark', () => {
    it('should return an ');
  });

  describe('the method getMarksForFile', () => {
    it('should return an ');
  });

  describe('the method getMarksForPage', () => {
    it('should return an ');
  });

  describe('the method getPageMarkDefinitions', () => {
    it('should return an ');
  });

  describe('the method getPageMarkDefinitionsByFileType', () => {
    it('should return an ');
  });

  describe('the method getPageMarksForFile', () => {
    it('should return an ');
  });
});
